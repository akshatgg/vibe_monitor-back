server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  # ==================== LOKI URL CONFIGURATION ====================
  # IMPORTANT: Update this based on where you're deploying

  # CURRENT: For local development or EC2-A → EC2-B (different servers)
  - url: http://54.67.106.73:3100/loki/api/v1/push

  # FOR EC2 DEPLOYMENT (same server): Uncomment below, comment above
  # - url: http://loki:3100/loki/api/v1/push

  # FOR EC2 DEPLOYMENT (different server): Use EC2-B's IP
  # - url: http://54.67.106.73:3100/loki/api/v1/push  # Use private IP if in same VPC
  # ================================================================

scrape_configs:
  # Scrape Docker container logs
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
        filters:
          - name: label
            values: ["logging=promtail"]

    relabel_configs:
      # Extract container name
      - source_labels: ['__meta_docker_container_name']
        regex: '/(.*)'
        target_label: 'container'

      # Extract job label
      - source_labels: ['__meta_docker_container_label_job']
        target_label: 'job'

      # Extract environment label
      - source_labels: ['__meta_docker_container_label_environment']
        target_label: 'environment'

      # Extract host label
      - source_labels: ['__meta_docker_container_label_host']
        target_label: 'host'

      # Extract log stream (stdout/stderr)
      - source_labels: ['__meta_docker_container_log_stream']
        target_label: 'stream'
