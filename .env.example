# General Environment
ENVIRONMENT=dev
API_BASE_URL=http://localhost:8000
WEB_APP_URL=https://vibemonitor.ai/

# Supabase (for prod use hosted Supabase)
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key
SUPABASE_DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# database URL for local only (when running outside Docker, use localhost:54322)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:54322/postgres

# CORS
ALLOWED_ORIGINS=["http://localhost:3000", "https://vibemonitor.ai"]

# JWT Settings
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production
CRYPTOGRAPHY_SECRET=your-super-secret-cryptography-key-for-token-encryption-32bytes

# Onboarding (Google OAuth)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret


# =======================
# Mailgun Configuration
# =======================
MAILGUN_API_KEY=your_mailgun_api_key
MAILGUN_DOMAIN_NAME=dummy.domain
# =======================
# Scheduler Authentication
# =======================
# Secret token for scheduler endpoints (GitHub Actions, cron jobs, etc.)
SCHEDULER_SECRET_TOKEN=your-super-secret-scheduler-token-change-this

# Log Level
LOG_LEVEL=INFO

# Slack Integration
SLACK_SIGNING_SECRET=YOUR_SLACK_SIGNING_SECRET
SLACK_WEBHOOK_URL=YOUR_SLACK_WEBHOOK_URL
SLACK_CLIENT_ID=YOUR_SLACK_CLIENT_ID
SLACK_CLIENT_SECRET=YOUR_SLACK_CLIENT_SECRET

# Groq
GROQ_API_KEY=your_groq_api_key_here
GROQ_LLM_MODEL=openai/gpt-oss-120b

# AWS SQS (LocalStack for development, real AWS for production)
AWS_REGION=us-east-1
SQS_QUEUE_URL=http://localhost:4566/000000000000/vm-api-queue

# AWS Credentials (for LocalStack use dummy values, not reqd for prod, prod works with IAM access)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Only required for local, dont put in prod
AWS_ENDPOINT_URL=http://localhost:4566

# Github app
GITHUB_APP_NAME=your_github_app_name
GITHUB_APP_ID=your_github_app_id
GITHUB_PRIVATE_KEY_PEM=your_base64_encoded_private_key
GITHUB_CLIENT_ID=your_github_app_client_id
GITHUB_WEBHOOK_SECRET=your_webhook_secret

# ==================== MONITORING CONFIGURATION ====================
# Loki/Promtail Configuration for sending logs to centralized Loki instance

# === FOR LOCAL DEVELOPMENT ===
# Sends logs to EC2 Loki instance (replace with your EC2 IP)
LOKI_URL=http://YOUR_EC2_IP:3100
DEPLOY_ENV=local
# HOSTNAME will be auto-detected, or set manually if needed
# HOSTNAME=my-local-machine

# === FOR EC2 DEPLOYMENT (VM-API on different EC2 than monitoring stack) ===
# When VM-API runs on EC2-A and Grafana/Prometheus/Loki run on EC2-B
# Uncomment these and comment out the LOCAL settings above:
# LOKI_URL=http://YOUR_EC2_IP:3100  # Use EC2-B's private IP if in same VPC for better performance
# DEPLOY_ENV=production
# HOSTNAME=ec2-vm-api-prod-1  # Give meaningful name to identify this server

# === FOR EC2 DEPLOYMENT (VM-API and monitoring on SAME EC2) ===
# If you run both VM-API and Loki/Prometheus on the same EC2:
# LOKI_URL=http://loki:3100  # Use Docker service name
# DEPLOY_ENV=production
# HOSTNAME=ec2-all-in-one

# Pushgateway URL for pushing Prometheus metrics
PUSHGATEWAY_URL=YOUR_EC2_IP:9091

# =======================
# Testing Configuration

WORKSPACE_ID=add_your_workspace_id_here_for_testing_purposes
